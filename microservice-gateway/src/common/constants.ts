export enum RabbitMQ {
  AuthQueue = 'auth',
}

export enum AuthMsg {
  LOGIN = 'login',
  REGISTER = 'register',
  GET_USER = 'get-user',
  UPDATE_USER = 'UPDATE_COMPRA',
  DELETE_USER = 'DELETE_COMPRA',
}
